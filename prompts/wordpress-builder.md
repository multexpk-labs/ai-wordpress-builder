# WordPress Builder Prompt

Use: DISCOVER -> AUDIT -> EXTRACT -> STRUCTURE -> PLAN -> GENERATE -> REVIEW -> BACKUP -> APPLY -> VERIFY.

Create a content model and page plan before changing the site. Use [CONTENT REQUIRED] when an important fact is missing. Never publish unreviewed organization-specific claims.
