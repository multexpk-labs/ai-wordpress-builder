# Security

Do not report credentials, application passwords, API keys, or private customer data in public issues.

The framework uses read-only defaults, backups, dry-runs, explicit apply steps, and verification to reduce production risk.

If a credential is exposed, rotate it immediately.
